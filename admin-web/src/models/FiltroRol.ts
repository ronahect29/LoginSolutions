export interface FiltroRol {
    id_rol: number;
    nombre: string;
    codigo: string;
}
export interface RolConfigUsuario {
    id_rol: number;
    nombre: string;
    codigo: string;
    default: boolean;
}